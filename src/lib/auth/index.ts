// Helpers de autenticación/autorización (NextAuth). Se implementan en 0.3.
// Expondrá: getSession(), requireRole('ADMIN' | 'STAFF'), y utilidades para
// verificar rol en SERVIDOR en cada acción sensible (sección 17.4).
export {};

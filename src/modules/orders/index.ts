// Módulo: orders — máquina de estados COMERCIAL del pedido
// (PENDING_PAYMENT → PAID → IN_PRODUCTION → READY_TO_SHIP → SHIPPED →
// DELIVERED | CANCELLED | REFUND_REQUESTED → REFUNDED). Transiciones validadas
// en código + historial (fecha, usuario, nota). (F1)
export {};

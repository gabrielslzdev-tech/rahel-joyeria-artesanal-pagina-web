export default function AccountPage() {
  // Placeholder del perfil del cliente (historial, direcciones, favoritos,
  // devoluciones, puntos F2). Se construye en Fase 1. Requiere sesión.
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Mi cuenta</h1>
      <p className="mt-2 text-neutral-600">Perfil del cliente — pendiente (F1).</p>
    </main>
  );
}
